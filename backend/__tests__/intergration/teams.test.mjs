describe("Team functions", () => {
  beforeEach(() => {
    // reset all mock
  });

  describe("getTeams", () => {
    it("Should be a to return a team information", () => {});
    it("Should be a to return an error", () => {});
  });
});

// TODO - make tests
