describe("Event functions", () => {
  beforeEach(() => {
    // reset all mock
  });

  describe("getEvents", () => {
    it("Should be a to return a list of events", () => {});
    it("Should be a to return an error", () => {});
  });
  describe("getEntries", () => {
    it("Should be a to return a list of entries", () => {});
    it("Should be a to return a list of entries where event is specified", () => {});
    it("Should be a to return an error", () => {});
  });
});
